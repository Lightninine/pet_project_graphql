from django.apps import AppConfig  # pragma: no cover


class MasterOfActsConfig(AppConfig):  # pragma: no cover
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'core'
    verbose_name = 'Ядро'
